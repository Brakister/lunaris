<Window x:Class="Lunaris.UI.Views.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Sobre — Lunaris"
        Width="380" Height="260"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow"
        Background="{DynamicResource BackgroundBrush}"
        Foreground="{DynamicResource PrimaryTextBrush}"
        FontFamily="Segoe UI">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="&#xE734;" FontFamily="Segoe MDL2 Assets" FontSize="36"
                   Foreground="{DynamicResource AccentBrush}" HorizontalAlignment="Center"/>

        <TextBlock Grid.Row="1" Text="Lunaris Launcher" FontSize="22" FontWeight="Bold"
                   HorizontalAlignment="Center" Margin="0,10,0,0"/>

        <TextBlock Grid.Row="2" Text="Versão 1.5.0" Foreground="{DynamicResource SecondaryTextBrush}"
                   HorizontalAlignment="Center" Margin="0,4,0,0"/>

        <TextBlock Grid.Row="3" Text="Um launcher nativo para Windows 10/11, rápido, leve e privado.
Tudo é processado localmente." FontSize="13"
                   Foreground="{DynamicResource SecondaryTextBrush}"
                   TextWrapping="Wrap" TextAlignment="Center"
                   VerticalAlignment="Center" LineHeight="22"/>

        <Button Grid.Row="4" Content="Fechar" Style="{StaticResource PrimaryButtonStyle}"
                HorizontalAlignment="Center" Width="120" Click="OnCloseClick"/>
    </Grid>
</Window>
